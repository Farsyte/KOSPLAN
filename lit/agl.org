#+PROPERTY: header-args :results none
#+PROPERTY: header-args :tangle ../Archive/agl.ks

agl - height above ground.

This is a modified radar altimeter that
provides how far the vessel is from
touching the ground.
  
#+begin_src ks
  set agl_box to ship:bounds.
  set agl to {return agl_box:bottomaltradar.}.
  on stage:number {set agl_box to ship:bounds. return true.}.
#+end_src
