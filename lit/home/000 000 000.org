#+PROPERTY: header-args :results none
#+PROPERTY: header-args :tangle ../../Archive/home/000 000 000/go.ks

go.ks -- GO script for 000 000 000 aka the Development Workstation

This script is a framework within which KOSPLAN code
can be designed, implemented, tested, and debugged.

* Import Required Packages

#+begin_src ks
  import("mnpro").
#+end_src

* During package import: set parameters

This function is executed by IMPORT when it
finds nothing more specific for this mission.

#+begin_src ks
#+end_src

* Provide a GO function.

This function is executed by IMPORT when it
finds nothing more specific for this mission.

#+begin_src ks
  set go to {
#+end_src

* do stuff

#+begin_src ks
  mpadd({
    set h to vcrs(body:position,ship:velocity:orbit).
    lock steering to lookdirup(h,-sun:position).
    return choose mpinc() if vang(steering:vector,facing:vector)<1 else 1.}).
  mncirc().
#+end_src

** this test thing always starts at the top of the plan when rebooting.

#+begin_src ks
  mpclr(). mprun().
#+end_src

* End the Program.

#+begin_src ks
  print "program complete. ABORT to reboot.".
  wait until abort. abort off. reboot.}.
#+end_src

